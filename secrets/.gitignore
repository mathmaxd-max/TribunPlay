# Keep this directory in the repo for your private notes and env files,
# but never commit real keys. Everything here is ignored except templates
# and this README.
*
!.gitignore
!README.md
!*.example
