# Copy to .env.local for local development (gitignored).
# Production: set the same variables in Cloudflare Pages → Environment variables.

# Optional overrides (defaults handle prod vs dev in src/config.ts)
# VITE_API_BASE=
# VITE_WS_BASE=

VITE_GOOGLE_CLIENT_ID=
VITE_TURNSTILE_SITE_KEY=
